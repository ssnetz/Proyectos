Este proyecto todavía no está en el pipeline de deploy automático.

Su arquitectura de build/servido es distinta a la de stock-control y
turnos-prioritarios (backend/index.php sirve los assets desde public/ en vez
de un frontend/dist/ genérico + .htaccess estándar), y ya está corriendo en
vivo en /var/www/html/fuel-control. Antes de conectarlo al deploy automático
hay que verificar junto con el usuario cómo Apache le llega el tráfico
realmente (no hay vhost propio, y su .htaccess actual no tiene las reglas de
rewrite que sí usan los otros dos proyectos) para no romper nada.

Por ahora esta carpeta solo sirve de respaldo versionado del código fuente.
Borrar este archivo cuando se confirme y adapte el deploy.
