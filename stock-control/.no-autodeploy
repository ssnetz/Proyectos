stock-control ya está corriendo en vivo en /var/www/html/farmacia,
desplegado a mano hasta ahora (tiene install.php/diag.php que no vienen del
repo). El pipeline ya está preparado para tomarlo (.deploy-target apunta a
"farmacia", .deploy-keep protege diag.php/install.php de --delete, y
config/database.php ya existe en el servidor con las credenciales reales asi
que no se pisa), pero falta confirmar con el usuario que el código de este
repo coincide con lo que hay en vivo (no se descarta que se hayan aplicado
cambios directo en el servidor que todavía no están en git) antes de activar
el primer sync automático.

Borrar este archivo cuando se confirme.
